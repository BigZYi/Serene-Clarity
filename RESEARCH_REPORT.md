# 研究性学习报告 —— 基于人脸表情识别的桌面情绪管理助手「心晴」

---

## 一、选题背景与意义

### 1.1 背景

近年来，大学生心理健康问题日益受到关注。学业压力、人际关系、离家思乡、就业焦虑等，使得不少学生长期处于亚健康情绪状态。然而，多数人不会主动寻求心理帮助——心理咨询需要预约，开口求助需要勇气。

与此同时，计算机视觉和大型语言模型（LLM）技术飞速发展：

- **MediaPipe** 可以在普通 CPU 上实时检测 478 个面部关键点；
- **DeepSeek** 等国产大模型能以极低成本生成自然的中文对话；
- **语音合成（TTS）** 可以让文字"开口说话"。

这让我们产生一个设想：**能否做一个"桌面陪伴型"的情绪管理助手——它不说话、不打扰，只在摄像头前默默守护，在你情绪低落时及时送上一句温暖的话？**

### 1.2 研究意义

- **现实意义**：为大学生提供低门槛、即时、无压力的情绪支持；
- **技术意义**：整合计算机视觉、大语言模型、语音合成、天气服务等多种技术，完成一次完整的多模态人机交互应用实践；
- **隐私意义**：探索"本地处理、不上传"的边缘计算方案，为隐私敏感场景提供参考。

【图片1：项目整体效果图（运行中的网页截图）】

---

## 二、研究目标

1. 实现基于 MediaPipe 的 **7 种面部表情**实时识别（微笑、悲伤、生气、惊讶、害怕、厌恶、中性）；
2. 将表情映射为 **4 类心理状态**（开心、情绪低落、焦虑、平静）；
3. 结合 **实时天气** 与 **时间感知**，让疏导内容更贴合场景；
4. 调用 **DeepSeek LLM** 生成温暖、自然、有分寸的中文疏导文案；
5. 可选 **语音播报**，用合成语音将疏导"说"出来；
6. 提供 **静默守护** 体验：摄像头默认折叠、暂停/恢复、主动获取等交互设计。

---

## 三、研究方法与过程

### 3.1 技术路线

```
摄像头 → MediaPipe 表情识别 → 心理状态分类 → 天气+时间 → DeepSeek LLM → 文字/语音输出
```

### 3.2 研究过程

#### 阶段一：技术调研与选型

对比了传统 OpenCV Haar 级联、YOLO 目标检测、MediaPipe Face Landmarker 三种人脸方案：

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| Haar 级联 | 轻量、快 | 只能框脸，无表情特征 | 不采用 |
| YOLO | 通用检测 | 需要 GPU/权重，表情需另接模型 | 不采用 |
| MediaPipe | 478点 + 52维表情系数，CPU可跑 | 正脸/光照依赖 | ✅ 采用 |

【图片2：MediaPipe 478 点人脸网格示意图（可运行 demo_5_FaceMesh.ipynb 后截图）】

#### 阶段二：表情识别模块（vision.py）

- 通过 MediaPipe Face Landmarker 提取 **478 个面部关键点** 与 **52 维 blendshape 表情系数**；
- 编写几何规则：嘴角上扬/下垂、眉毛高低、张嘴程度等；
- 输出 7 种表情，并支持 **多人脸检测**（多人时返回"陪伴"状态）。

【图片3：表情识别规则示意图（几何特征→表情的流程图）】

#### 阶段三：心理状态分析（core.py）

- 用 **30 帧滑动窗口** 累积情绪，避免单帧抖动；
- 表情 → 心理状态映射表（如 微笑→开心、悲伤→情绪低落、生气→焦虑）；
- 计算主导情绪与情绪分布，生成"近期情绪摘要"供 LLM 使用。

#### 阶段四：LLM 疏导生成（llm.py）

- 设计 **系统提示词（System Prompt）**：定位为"桌面 AI 情绪管理助手"，面向在校大学生；
- 注入表情、心理状态、近期情绪、**实时天气**、**当前时段** 等上下文；
- 本地规则文案作为 **降级兜底**（API 不可用时仍能工作）。

#### 阶段五：语音播报（broadcast.py，可选）

- 集成华为云 VCS 语音合成，将疏导文案转为音频；
- 设计了音频防重复播报机制，避免 Gradio 组件刷新打断播放。

#### 阶段六：界面与交互（app.py）

- 使用 **Gradio 6.x** 搭建网页界面；
- **静默守护** 设计：摄像头折叠在底部、仅本机分析、不上传不存储；
- 提供"暂停守护"、"主动获取疏导"、"重置"等控制；
- 拍立得模式：每 5 秒分析一帧，降低心理压迫感。

【图片4：系统架构图（六大模块关系图）】

---

## 四、研究成果

### 4.1 功能实现

| 功能 | 实现情况 |
|------|----------|
| 表情识别 | ✅ 7 种表情，多人脸检测 |
| 心理状态分析 | ✅ 4 类心理状态 + 情绪累积 |
| 天气联动 | ✅ 每日缓存，融入 LLM 上下文 |
| 时间感知 | ✅ 深夜/白天不同语气 |
| LLM 疏导 | ✅ DeepSeek API + 本地降级 |
| 语音播报 | ✅ 华为云 VCS（可选） |
| 静默守护 | ✅ 摄像头折叠 + 暂停 + 主动触发 |

### 4.2 创新点

1. **静默守护交互范式**：用户零操作，摄像头折叠隐藏，减少"被盯着"的压迫感；
2. **拍立得分析模式**：每 5 秒取一帧分析，替代连续流式，更省资源更安心；
3. **多模态上下文融合**：表情 + 心理 + 天气 + 时间共同塑造疏导内容；
4. **隐私优先**：画面仅本机内存处理，不落盘、不上传。

【图片5：运行结果截图（表情/心理/天气/疏导四卡片）】

---

## 五、遇到的问题与解决

### 5.1 Gradio 版本兼容

Gradio 6.0 移除了 `bubble_full_width` 等参数、要求 `theme/css` 传入 `launch()`。通过查阅更新日志逐一适配。

### 5.2 语音重复播报

Timer 每秒刷新导致 Audio 组件重复触发。通过记录"已发送音频路径"并比较变化来修复。

### 5.3 疏导内容模板化

LLM 回复千篇一律。通过扩展提示词、增加随机聊天角度、强调"每次不同"来改善。

### 5.4 天气数据闪变

启动时天气正常、随后变为"不可用"。排查发现是局部变量作用域问题，将天气获取统一到回调开头。

---

## 六、总结与展望

### 6.1 总结

本项目完成了一个"从表情到心情再到陪伴"的完整闭环：MediaPipe 读懂表情 → 规则引擎判断心理 → 天气与时间丰富上下文 → LLM 生成有温度的疏导 →（可选）语音播报。整个系统以"静默守护、隐私优先"为设计核心，探索了 AI 在情绪支持场景下的落地形态。

### 6.2 展望

| 方向 | 说明 |
|------|------|
| 本地化 LLM | 用 Ollama + Qwen 替代云 API，彻底离线 |
| 长期追踪 | 记录日/周情绪趋势，生成情绪报告 |
| 多模态输入 | 加入语音输入（ASR），真正"开口聊天" |
| 移动端 | ONNX 模型 + 轻量推理，部署到手机 |
| 安全审核 | 接入内容安全 API，确保疏导内容无害 |

【图片6：未来方向路线图（可选）】

---

## 附：运行方式

```bash
pip install -r requirements.txt
python app.py
# 浏览器打开 http://localhost:7860
```

---

## 附图位置说明

| 编号 | 建议图片 | 获取方式 |
|------|----------|----------|
| 图片1 | 网页整体运行截图 | 启动 app.py 后截全屏 |
| 图片2 | MediaPipe 478 点人脸网格 | 运行 `Day2/MediaPipe_stu/demo_5_FaceMesh.ipynb` 截图 |
| 图片3 | 表情识别流程图 | 用 draw.io / PPT 绘制 |
| 图片4 | 系统架构图 | 用 draw.io / Mermaid 绘制 |
| 图片5 | 四卡片运行截图 | 启动后等待一次疏导触发后截图 |
| 图片6 | 未来方向路线图 | 用 PPT 简单绘制，可选 |

## 七、关于 AI 辅助开发（写给中学生的说明）

本项目使用了一种叫做 **Vibe Coding（氛围编程）** 的方式开发：
开发者用自然语言描述需求，由 AI 编程助手（如 GitHub Copilot）
生成大部分代码，人类负责构思功能、检查代码、修复 bug。

- **不是"一键生成"**：AI 生成的代码经常有 bug（比如语音重复播报、
  天气闪变），都需要人工逐一排查修复；
- **需要基础**：只有懂一点 Python，才能看懂 AI 写的代码、
  知道哪里出错、改哪里；
- **也需要硬件与配置**：摄像头、Python 环境、API 密钥、
  模型文件，缺一样程序都跑不起来。

所以即使把整个项目文件夹发给一个同学，对方也可能因为
环境不齐而无法运行——这不是代码的问题，而是**运行环境**的问题。

> 💡 **给中学生的启发**：AI 能帮你"写得快"，但不能替你"想得清"。
> 真正的创造仍然来自你对问题的理解、对用户需求的洞察。